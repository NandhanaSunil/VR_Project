// Note: No "using UnityEngine" is needed for a simple interface.

public interface IInteractable
{
    // Any class that implements this interface MUST have an Interact() method.
    void Interact();
}